function Player ({name, team}) {
    this.name = name;
    this.team = team;
}

/**
 * TODO
 * Create correlations based on what a player stats for given items (weather, location, venue size, etc.)
 */

module.exports = Player;